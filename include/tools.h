#ifndef TOOLS_H
#define TOOLS_H
#include "defines.h"
#include <ArduinoJson.h>

int hexToInt(char c);
int hexToInt(char c);
String getTime();
String zeroPadding(String p_input, unsigned int p_tot_digits);

#endif // TOOLS_H
